a.setSmooth(0.8)

osc(18, 0.1, 0)// .color(() => a.fft[2]*10, () => a.fft[1]*10, ({time}) => time % 10)
.mult(osc(10, 0.01, 0))
.repeat(2, 20)
.modulate(o1)
.rotate(() => a.ftt[0]*4)
.scale(1, () => (a.fft[0]*0.9 + 2)).diff(o1)
.out(o0)
shape(4, () => a.fft[0] + 0.3, 0.01)
.color(({time}) => time % 4, ({time}) => time % 7, ({time}) => time % 10)
.blend(o0)
.mult(osc(40)).modulateRotate(o0, 0.2)
.rotate(0.2).diff(o0).out(o1)

do
  bpm 102
  d1
    $ chunk 3 (sply (choose [2, 3, 6]))
    $ chunk 7 (stut 2 0.9 "s")
    $ while "<f!2 [f t]>" (stutWith 3 "e" ((* sp 4).(# pan "[0|1]").(# hp (rrand 1200 5000) (slow 4 tri/3))))
    $ sh "3" ((# coarse 2).(|* g 0.7).(# pan perlin))
    $ sh "3h" (# ac (rand*8))
    $ sh "q" (# boo)
    $ striateBy "5" 0.9
    $ s "dald:0"
    # shape 0.6
    # lpf (range 100 8000 $ slow 3 $ sin)
    # lpq (slow 4 $ saw)

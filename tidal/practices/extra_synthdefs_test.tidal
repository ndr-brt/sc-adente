
d1
    $ stut' 4 0.25 (#speed (slow 8 $ sine))
    $ every 8 (off 0.25 ( 0.125 ~>))
    $ within (0, 1.25) (fast 4)
    $ jux (#up 4)
    $ slow 2
    $ foldEvery[8, 16, 24] (rev)
    $ chop (choose [50, 4, 6, 8, 10, 12]*2)
    $ s "percuo"
    # lpf (range 80 3000 $ slow 2 $ sine )
    # resonance 0.4

hush

d1
    $ n (fromIntegral <$> markovPat 4 3 [
        [0.25, 0.45, 0.3],
        [0.3, 0, 0.7],
        [0.7, 0.3, 0],
        [0.15, 0.39, 0.46]
    ])
    # s "plucking"
    # legato 2
    # room 0.2
    # size 0.8

    # delay 0.34
    # delayt 0.23
    # delayfb 0.33

hush

d2
    $ slow 2
    $ stack [
        struct (binary "19") $ s "kick_oto309",
        struct (binary "79") $ s "hihat" # gain 0.8,
        struct (binary "121") $ s "snare_stein"
    ]
    # nudge 0.4

d2 silence

d2
$ s "benjolis"
# oscA "400"
# oscB "25"
# runB 0.9
# runA (range 0 1 $ slow 8 $ sine)
# runF (range 0 1 $ slow 8 $ cosine)
# gain 0.9
# loop 45
# res 0.6
# offset 0.4
# freq (range 80 160 $ slow 16 $ cosine)
# lp' 1200 0.01
# n 8
# legato 2


-- marunn che flou

d1
  $ every 4 (iter 2)
  $ every 6 (shuffle 2)
  $ rarely (brak)
  $ s "[kk kk*2 [~ ss]*2, hh(5,8)]"
  # del' 0.12 0.1 "0 0.5"
  # rvb' "0.2 0.04" "0.41 0.1"
  # distort "0.6"
  # nudge 0.4


d1
  $ every 6 (iter 3)
  $ every 4 (linger 0.25)
  $ stack [
    s "[kk*2? ss]*2",
    sometimes (# del' "0.67" "0.34" "0.12") $ s "hh(5,8)"
  ]
  # nudge 0.4
  # rvb' "0.015" "0.1"

d1 silence

d2
  $ s "babbl"
  # n (scale "minor" (irand 2))
  # nudge 0.4
  # legato 2
  # pan perlin

d2 silence

once
  $ n (run 3 * 3.4)
  # s "goeyfx*2"
  # nudge 0.4
  # legato 2

d4
  $ every 4 (|+| n 2)
  $ rarely (stut 4 0.5 0.2)
  $ n (scale "minor" "0 4 5 6")
  # s "blowbotl"
  # gain 0.9
  # nudge 0.4
  # rvb' 0.04 0.2

d4 silence

d5
  $ every 4 (# n (scale "minor" "3 2 0 7 8 ~!3"))
  $ n (scale "minor" "6 8 ~!2 5 4 ~!2 [5 7]*2")
  # s "spook2"
  # octave 3
  # gain 1
  # pan rand
  # nudge 0.4

d5 silence

hush

-- cappraiana

d1
  $ juxBy (range 0.1 0.9 $ slow 13 $ cosine) (# octave 2)
  $ every 5 (# s "flute*24")
  $ s "flute*14"
  # n (scale "kumai" (irand 8))
  # octave 1
  # nudge 0.4
  # legato 4
  # gain 0.9
  # lpf (range 700 4600 $ slow 4 $ sine)

d2
  $ slow 2
  $ whenmod 7 5 (brak)
  $ rarely (# del' 0.56 0.23 0.15)
  $ "[kk*2 ~ kk ss*2?, hh(5,8)]"
  # nudge 0.4
  # distort (range 0.1 0.4 $ slow 11 $ saw)
  # shape 0.3
  # crush 5

d3
  $ every 11 (# crush 5)
  $ whenmod 10 7 (# n 0)
  $ whenmod 14 12 (|+| n 3)
  $ s "werkit"
  # n 2
  # octave 4
  # crush 7

d4
  $ s "spook2/7"
  # n (scale "kumai" (irand 8))
  # octave 7
  # pan rand
  # release 4
  # waveloss 25
  # rvb' 0.1 0.4

hush

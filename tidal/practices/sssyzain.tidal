d1
  $ superimpose (
      (|+ octave 2)
      .(slow 4)
      .(# legato 1)
      .(rarely (|- octave 1))
      .(# rmod' 0.7 44 0.7)
      .(# ar' 0.01 0.8)
  )
  $ s "addi*8"
  # legato 4
  # n (irand 14)
  # octave "3"
  # rvb' 0.4 0.8
  # shape (rand/4)

d2
  $ superimpose (
      (# octave 3)
      .(# hp' 40 0.6)
      .(|* ar' "4" "4")
      .(# legato 2)
  )
  $ n (scale "superLocrian" (segment 12 $ irand 12))
  # "sbaum"
  # octave "[5|6|7]"
  # ar' 0.05 0.2
  # rvb' 0.4 0.6

hush
